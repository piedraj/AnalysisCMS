#if !defined(MYLIB_CUTSPR_H)
#define MYLIB_CUTSPR_H 1

#include "TString.h"

enum {
  ncut  // This line should be always last                                                                                                                  
};

const TString scut[ncut] = {
};

#endif 
